import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'TabTab',
  appName: 'TabTab',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
